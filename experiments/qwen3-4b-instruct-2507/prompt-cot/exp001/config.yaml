# Config RIÊNG của một thí nghiệm. Đây là lớp CUỐI khi hợp nhất, nên nó đè được mọi lớp trước
# (model, repo, task, evaluation, training, tracking). Xem docs/05_config/06_experiment.md.
#
# Chỉ khai những gì KHÁC mặc định dùng chung. Khai lại một giá trị y hệt mặc định là che mất
# nguồn sự thật: người đọc sau không biết sửa file nào mới đúng.

exp_id: exp001

# Thí nghiệm này làm lại từ thí nghiệm nào (đường dẫn đầy đủ `<model>/<method>/<expNNN>`),
# null nếu là bản gốc. Dùng khi cần đối chiếu với một lần chạy trước.
parent: null

# `model` phải trùng tên file configs/models/<model_id>.yaml; `method` cũng là tên thư mục cha.
model: qwen3-4b-instruct-2507
method: prompt-cot

data:
  # MỘT dataset duy nhất. Khai danh sách là lỗi: hai bộ dữ liệu khác nhau thì không so được.
  dataset: cosmetics
  version: v0.1.0

  # Bắt buộc khai, KHÔNG kế thừa: mỗi vai dùng split nào của chính dataset đó.
  # `eval` luôn cần (để biết chấm trên tập nào). Trỏ `eval` vào `train` bị chặn vì rò rỉ dữ liệu.
  # Khai `train` và `val` khi training.enabled: true.
  #
  # exp001 chấm trên TEST - đây là con số để so với công bố tham chiếu. Lúc dựng đường chạy, các lượt
  # chạy kiểm tra dùng `val` (tập LỰA CHỌN: chọn prompt, số ví dụ, ngưỡng cắt). Chọn theo test là tự
  # lừa mình, nên test chỉ chạy khi cấu hình đã chốt và kết quả chỉ dùng MỘT lần.
  roles: {eval: test}

# Số mẫu dùng để chấm, đè `n` của lớp evaluation. null = CẢ split: so với công bố thì phải chấm hết
# tập test, chấm một tập con rồi đem so là so hai phép đo khác nhau.
n: null

# Prompt lấy từ THƯ VIỆN DÙNG CHUNG, không phải file riêng của thí nghiệm: đây là prompt CoT của
# phương pháp `prompt-cot` (chuỗi suy luận + 2 ví dụ few-shot). Đường dẫn tính từ thư mục thí nghiệm
# trước, rồi tới gốc repo: từ đây lên gốc repo là BỐN cấp (`<model>/<method>/<expNNN>` nằm trong
# `experiments/`). Prompt khai bằng ĐƯỜNG DẪN thì file ví dụ phải khai TƯỜNG MINH: suy ra theo tên
# chỉ đúng khi prompt gọi bằng tên trong thư viện.
prompt: ../../../../configs/prompts/absa_cot_v1.txt
examples: ../../../../configs/prompts/examples/absa_cot_v1.txt

# Chỉ khai khi cần:
# system_prompt: ../../../../configs/prompts/system/absa_cot.txt   # khi prompt có {system_prompt}
# requires_extra:                             # thứ notebook phải kiểm mà máy không suy ra được
#   - ../../../../data/models/Qwen3-0.6B      # đường dẫn cũng tính từ thư mục thí nghiệm trước
